# Update instructions for the cron job

This is a note file, not a script — the cron job runs as an LLM-driven prompt (see cron job "Quant Lab Dashboard — Auto Update") because it needs to call trader.dev MCP tools, not just shell commands.

Repo path: ~/.hermes/quant-lab-dashboard
Live URL: https://ali-sportstech.github.io/quant-lab-dashboard/
